var funadd ={
    nextId: 1,
    cache = {},
    add: function fn(fn){
        if(!fn.id){
            fn.id = this.nextId++;
            this.cache[id] = fun;
            return true;
        }
    }
};